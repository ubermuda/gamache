# Demo skill

Prose is not a command: this is just a moment of English, and you should just
the read failing spec rather than the whole suite. Inline it, it is just plumbing.

Mid-command the word is not a recipe name either: `bin/console debug:router | grep just`.

Only a command position counts: `just cs`.
